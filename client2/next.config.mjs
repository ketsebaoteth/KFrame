/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "static7.tgstat.ru", "res.cloudinary.com"], // List of allowed domains for images
  },
};

export default nextConfig;
